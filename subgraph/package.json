{
  "name": "simple-event-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for SimpleEventContract",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/",
    "create-local": "graph create --node http://localhost:8020/ simple-event-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ simple-event-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 simple-event-subgraph"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.38.1"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
